#!/bin/bash

mkdir -p ${WRF4G_LOCALSCP}/WRFV3/run
cd  ${WRF4G_LOCALSCP}/WRFV3/run
for file in `ls ${WRF4G_ROOTDIR}/runshared/ | grep -v met_em`
do 
  ln -s ${WRF4G_ROOTDIR}/runshared/$file .
done
cd -

